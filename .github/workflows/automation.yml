name: Smoke CI

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show Python
        run: |
          python3 --version
          uname -a
      - name: Sleep short
        run: |
          echo "Smoke test OK"
